[project]
name = "kyogre"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Manish Bisht", email = "manish@delightree.com" }
]
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
    "streamlit>=1.40.0",
    "beautifulsoup4>=4.12.0",
]

[project.scripts]
kyogre = "kyogre:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
